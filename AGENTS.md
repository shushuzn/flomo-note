项目总则（flomo-note）

本文档是项目总则，只约定顶层目标与全局不可违背的约定；具体执行细则一律以 .kilo/skills/flomo-note/SKILL.md 为准（唯一细则源），两者不重复记录，本文件不复制 SKILL 细节。

项目是什么

flomo 极简卡片笔记技能：把网页、文章、想法整理成条条 flomo memo，直接写入用户云端 flomo 账号。卡片形态 = 一行标签段 + 简短正文，一事一卡、标签聚合。

顶层目标

- 把看到的好内容高效沉淀成云端 flomo 卡片，为往后的回顾与复用服务。
- 记录足够轻、足够快；格式统一、可被工具稳定读写。

核心约定（全局不可违背）

1. 笔记只在云端：卡片正文只存在于 flomo 账号；本地 d:\OpenClaw\flomo-note 不落盘任何笔记内容，绝不以"本地文件"代存云端卡片。
2. 本地只留工具：本目录仅存放技能文档（SKILL.md）、本总则（AGENTS.md）与 MCP 配置（.mcp.json）。git 只追踪这些技能/配置文档，不含笔记内容；.mcp.json 含 token，已 gitignore 不入库。
3. 写云需授权：一切对云端数据的写操作（建 memo、更新、改标签）都在动手前把内容/改动展示给用户并征得同意，绝不静默写云端。
4. 来源实实在在：卡片引用的 URL 必须真实、可核、经过抓取或核验；禁止生造来源。
5. 细则听 SKILL：卡片格式、标签规则、记录流程、执行铁律等可执行细节，唯一以 SKILL.md 为准，本文件不掺演、不复制。
6. 收口成闭环：每轮工作结束前把该轮做完整、可交付；有跨轮事项必须明示，不静默丢弃；每次收口必做第 7 步复盘——一次同时输出常规 + 创新两条建议并征询是否落地。

目录分工（职责分明）

- AGENTS.md —— 项目总则（本文件）
- .kilo/skills/flomo-note/SKILL.md —— 执行细则（唯一细则源）
- .mcp.json —— flomo MCP 配置（含 Bearer token，已忽略）
- 笔记实体 —— 只在云端 flomo，本地不存在